<?xml version="1.0"?>
<ruleset name="APIM coding standard">
    <description>APIM coding standard</description>

    <!-- display progress and sniff -->
    <arg value="p"/>
    <arg value="s"/>

    <!-- use colors in output -->
    <arg name="colors"/>

    <!-- inherit rules from: -->
    <rule ref="PSR2"/>

    <!-- Paths and file to check -->
    <file>framework</file>

    <!-- specific and just exclude rules in development files -->
    <rule ref="PSR2.Methods.MethodDeclaration.Underscore">
        <exclude-pattern>tests</exclude-pattern>
    </rule>
    <!--<rule ref="PSR1.Classes.ClassDeclaration.MissingNamespace">-->
        <!--<exclude-pattern>database</exclude-pattern>-->
        <!--<exclude-pattern>tests</exclude-pattern>-->
    <!--</rule>-->

    <!-- specific and just exclude rules in some file -->
</ruleset>
